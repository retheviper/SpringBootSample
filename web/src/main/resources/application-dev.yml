spring:
  h2:
    console:
      enabled: true
      path: /test_db
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
  jpa:
    database: h2
    show-sql: true
#    generate-ddl: true
    hibernate:
#      ddl-auto: create-drop
      ddl-auto: none